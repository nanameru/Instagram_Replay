# Instagram DM 履歴取得機能

## 概要

Instagram DMの履歴メッセージを取得し、会話ビューに表示する機能を実装しました。この機能は以下の特徴を持っています：

1. **ページネーション対応**: 「過去のメッセージを読み込む」ボタンで古いメッセージを順次読み込めます
2. **モックデータフォールバック**: Instagram APIの権限制限（`read_mailbox`権限が必要）に対応するため、APIアクセスが制限されている場合は自動的にモックデータを表示
3. **日本語ローカライズ**: 日付表示やエラーメッセージなどを日本語で表示
4. **エラーハンドリング**: API接続エラーや権限エラーを適切に処理し、ユーザーに分かりやすく表示

## 実装詳細

### API エンドポイント

`/api/instagram/historical-messages` エンドポイントを実装し、以下の機能を提供：

- 会話IDに基づくメッセージの取得
- ページネーションパラメータ（before/after）のサポート
- モック会話ID（`conv_`で始まるID）の自動検出とモックデータ提供
- Instagram APIの権限エラー（コード298）の検出とモックデータへのフォールバック

### フロントエンド コンポーネント

`MessageHistory` コンポーネントを実装し、以下の機能を提供：

- メッセージの取得と表示
- 「過去のメッセージを読み込む」ボタンによるページネーション
- モックデータ使用時の通知バナー表示
- 日本語ローカライズされた日付表示
- 送信者に基づくメッセージの左右配置
- エラー状態の適切な処理と表示

## 使用方法

1. 会話一覧から会話を選択
2. 会話詳細画面でメッセージ履歴を確認
3. 「過去のメッセージを読み込む」ボタンをクリックして古いメッセージを読み込む

## 制限事項

Instagram APIの制限により、実際のDMデータにアクセスするには `read_mailbox` 権限が必要です。この権限がない場合、アプリケーションは自動的にモックデータを表示します。モックデータ使用時は画面上部に通知バナーが表示されます。

## テスト結果

複数のテストスクリプトを使用して機能を検証しました：

### 基本機能テスト
```bash
node test-historical-messages-api.js
```
- ✅ モック会話IDが正しく検出される
- ✅ モックデータが正しく返される
- ✅ ページネーションリンクが正しく生成される

### ページネーションテスト
```bash
node test-historical-dms-with-pagination.js
```
- ✅ 「次のページ」リンクが機能する
- ✅ 「前のページ」リンクが機能する
- ✅ ページネーションデータが正しく維持される

### UI統合テスト
- ✅ 会話詳細画面でメッセージが正しく表示される
- ✅ 「過去のメッセージを読み込む」ボタンが機能する
- ✅ モックデータ使用時に通知バナーが表示される
- ✅ エラー状態が適切に処理される

すべてのテストが正常に完了し、機能が期待通りに動作していることを確認しました。
